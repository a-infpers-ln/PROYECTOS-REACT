import { Expense } from "../types"

type ExpenseDetailProps = {
    expense : Expensee
}

export default function ExpensDetail({expense} : ExpenseDetailProps) {
  return (
    <div>ExpensDetail</div>
  )
}
